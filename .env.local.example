# ===== 本地测试配置(复制为 .env.local 后填写, 已被 .gitignore 忽略) =====
# `npm run test:e2e` 会用这里的真实 key 打一遍 /v1/search /v1/news /v1/crawl /mcp

# 至少一个搜索 provider
SEARCH1API_KEY=
# TAVILY_KEY=
# SERPER_KEY=

# 可选: 同时测试兼容旧版的聊天代理(非流式 + 流式)
# APIBASE=https://api.openai.com/v1
# TEST_OPENAI_KEY=
# TEST_MODEL=gpt-4o-mini
